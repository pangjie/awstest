name: Deploy production
run-name: Deploy ${{ github.event_name == 'workflow_dispatch' && 'production-alt (manual)' || 'production (automatic)' }}

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  pull-requests: read

concurrency:
  group: deploy-${{ github.event_name == 'workflow_dispatch' && 'production-alt' || 'production' }}
  cancel-in-progress: false

jobs:
  deploy:
    if: github.ref == 'refs/heads/main'
    name: Build ARM image and deploy through SSM
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: ${{ github.event_name == 'workflow_dispatch' && 'production-alt' || 'production' }}
    env:
      AWS_REGION: ${{ vars.AWS_REGION }}
      ECR_REPOSITORY: ${{ vars.ECR_REPOSITORY }}
      AWS_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}

    steps:
      - name: Validate isolated deployment configuration
        env:
          AWS_ROLE_ARN: ${{ vars.AWS_ROLE_ARN }}
          EC2_INSTANCE_ID: ${{ vars.EC2_INSTANCE_ID }}
          DEPLOY_EVENT: ${{ github.event_name }}
        run: |
          set -euo pipefail
          : "${AWS_REGION:?Set AWS_REGION in this GitHub environment}"
          : "${ECR_REPOSITORY:?Set ECR_REPOSITORY in this GitHub environment}"
          : "${EC2_INSTANCE_ID:?Set EC2_INSTANCE_ID in this GitHub environment}"
          [[ "${AWS_ACCOUNT_ID}" =~ ^[0-9]{12}$ ]] || { echo '::error::Set a valid AWS_ACCOUNT_ID'; exit 1; }
          [[ "${AWS_ROLE_ARN}" == "arn:aws:iam::${AWS_ACCOUNT_ID}:role/"* ]] || { echo '::error::Deploy role must belong to the configured account'; exit 1; }
          if [[ "${DEPLOY_EVENT}" == workflow_dispatch && "${AWS_ACCOUNT_ID}" == 831823988119 ]]; then
            echo '::error::production-alt must use a different AWS account from production'
            exit 1
          fi

      - name: Verify the revision came from a merged pull request
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          merged_pr_count="$(gh api \
            -H 'Accept: application/vnd.github+json' \
            "/repos/${GITHUB_REPOSITORY}/commits/${GITHUB_SHA}/pulls" \
            --jq '[.[] | select(.base.ref == "main" and .merged_at != null)] | length')"
          if [ "${merged_pr_count}" -lt 1 ]; then
            echo '::error::Production deployment is allowed only for a commit associated with a merged pull request into main.'
            exit 1
          fi

      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm

      - name: Test exact revision
        run: npm ci && npm run check

      - name: Configure short-lived AWS credentials
        uses: aws-actions/configure-aws-credentials@e6de054238d6b7531b4efff3b6587d9aade6a06c # v6.2.3
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          allowed-account-ids: ${{ env.AWS_ACCOUNT_ID }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: github-${{ github.run_id }}

      - name: Sign in to Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@03f1aad4c6c7ffd436567f42f9384779290529bd # v2

      - name: Check whether this immutable image already exists
        id: image
        run: |
          if aws ecr describe-images \
            --repository-name "${ECR_REPOSITORY}" \
            --image-ids "imageTag=${GITHUB_SHA}" >/dev/null 2>&1; then
            echo 'exists=true' >> "${GITHUB_OUTPUT}"
          else
            echo 'exists=false' >> "${GITHUB_OUTPUT}"
          fi

      - name: Enable ARM emulation
        if: steps.image.outputs.exists != 'true'
        uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4.2.0

      - name: Set up Docker Buildx
        if: steps.image.outputs.exists != 'true'
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Build and push immutable ARM image
        if: steps.image.outputs.exists != 'true'
        id: build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          platforms: linux/arm64
          push: true
          tags: ${{ steps.ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # ECR basic scanning scans the runnable image manifest, while Buildx
          # attestations wrap a single-platform image in an OCI index.
          provenance: false
          sbom: false

      - name: Wait for ECR basic vulnerability scan
        run: |
          aws ecr wait image-scan-complete \
            --repository-name "${ECR_REPOSITORY}" \
            --image-id "imageTag=${GITHUB_SHA}"
          aws ecr describe-image-scan-findings \
            --repository-name "${ECR_REPOSITORY}" \
            --image-id "imageTag=${GITHUB_SHA}" \
            --query '{Status:imageScanStatus.status,Severity:imageScanFindings.findingSeverityCounts}'

      - name: Deploy and wait for readiness
        env:
          EC2_INSTANCE_ID: ${{ vars.EC2_INSTANCE_ID }}
          IMAGE_URI: ${{ steps.ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}
        run: |
          set -euo pipefail
          parameters="$(jq -cn --arg command "sudo flock --wait 300 /run/lock/aws-miniflow-deploy.lock /usr/local/bin/deploy-miniflow ${IMAGE_URI}" '{commands: [$command]}')"
          command_id="$(aws ssm send-command \
            --instance-ids "${EC2_INSTANCE_ID}" \
            --document-name AWS-RunShellScript \
            --parameters "${parameters}" \
            --comment "Deploy ${GITHUB_SHA}" \
            --query 'Command.CommandId' \
            --output text)"

          echo "SSM command: ${command_id}"
          set +e
          aws ssm wait command-executed \
            --command-id "${command_id}" \
            --instance-id "${EC2_INSTANCE_ID}"
          wait_status=$?
          set -e

          aws ssm get-command-invocation \
            --command-id "${command_id}" \
            --instance-id "${EC2_INSTANCE_ID}" \
            --query '{Status:Status,Output:StandardOutputContent,Error:StandardErrorContent}'

          exit "${wait_status}"
