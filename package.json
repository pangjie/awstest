{
  "name": "aws-miniflow",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal Codex to GitHub to EC2 to RDS delivery flow.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "preview": "node scripts/preview.js",
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/db.js && node --check public/app.js && npm test"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
